# ============================================
# Prometheus - 强化学习框架依赖管理
# ============================================
#
# 这个文件列出了项目需要的所有 Python 包
# 安装方式: pip install -r requirements.txt

# 深度学习框架 - 核心依赖
# PyTorch 是目前最流行的深度学习框架，类似的有 TensorFlow、JAX
# 它提供: 张量计算、自动微分、神经网络层等
torch>=2.0.0

# Gymnasium - 强化学习环境标准接口
# 这是 OpenAI Gym 的继承者，提供了统一的 RL 环境接口
# 比如: CartPole(小车倒立摆)、Atari 游戏、机器人控制等
gymnasium>=0.29.0

# NumPy - 数值计算基础库
# 提供: 高维数组、线性代数运算、傅里叶变换等
# PyTorch 的张量和 NumPy 数组可以互相转换
numpy>=1.24.0

# Matplotlib - 绘图库
# 用于: 绘制训练曲线、可视化奖励变化等
matplotlib>=3.7.0

# Pillow - 图像处理库
# 用于: 处理 Atari 游戏的图像观测
Pillow>=10.0.0

# ============================================
# 可选依赖（后续可能需要）
# ============================================
# tensorboard>=2.14.0   # 训练可视化
# wandb>=0.15.0         # 在线实验追踪
# pytest>=7.4.0         # 单元测试
